apiVersion: v1
kind: Service
metadata:
  name: hpa-demo-nodeport
spec:
  type: NodePort
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30000  # Specify the NodePort value you prefer
  selector:
    run: hpa-demo-deployment
